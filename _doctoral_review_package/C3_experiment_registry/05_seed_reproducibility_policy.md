# Seed Reproducibility Policy

Each experiment must define and persist:

- random seed
- train test split seed
- cross validation seed
- model initialization seed
- library versions

Default seed for baseline experiments:

42

If a seed changes, the experiment id must change.
